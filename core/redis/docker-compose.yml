name: redis

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    networks:
      - lab-network
    command: redis-server --appendonly yes

networks:
  lab-network:
    external: true
